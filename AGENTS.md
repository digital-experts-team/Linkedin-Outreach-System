# Custom Project Instructions

## Git & Deployment Rules
- **Do not push automatically**: Never run `git push` without explicit confirmation.
- Always ask the user for permission before pushing any changes to remote Git repositories (e.g. GitHub/origin).
